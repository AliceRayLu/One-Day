import { _decorator, Component, Node } from 'cc';
import { SceneManager } from './SceneManager';
const { ccclass, property } = _decorator;

@ccclass('ActS1Manager')
export class ActS1Manager extends SceneManager {
    start() {

    }

    update(deltaTime: number) {
        
    }
}

