import { _decorator, Component, Node } from 'cc';
import { SceneManager } from './SceneManager';
const { ccclass, property } = _decorator;

@ccclass('Lib_clockManager')
export class Lib_clockManager extends SceneManager {
    
}

